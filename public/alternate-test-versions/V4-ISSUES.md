# Issues

## `index-v4.html` is the latest iteration

- [ ] Persist lines on screen is still somewhat broken.
- [ ] Cfg dropdown UI runs off bottom of screen and doesnt allow scroll.
- [ ] Unsure if exported video removes UI as its intended to do.